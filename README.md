# Fiksy9790707-python-learning
# Python Learning Journey 🐍

> 记录我学习廖雪峰 Python 教程的代码和心得。

## 📅 学习进度 (Current Progress)

### 1. 基础部分 (Basics)
- [√] 输入输出与数据类型 (`input`, `float`)
- [√] 条件判断 (`if-elif-else`, BMI计算器)
- [√] 循环 (`for`, `while`)
- [√] **[重要]** 字典与集合原理 (Hash, Immutable Key)
- [√] **[重要]** 字符串与列表的方法区别 (`sort` vs `replace`)

### 2. 函数 (Functions)
- [√] 函数定义与调用
- [√] 函数参数魔法
    - 必选参数、默认参数 (坑: 可变对象)
    - 可变参数 `*args` (Tuple)
    - 关键字参数 `**kw` (Dict)
- [√] 递归函数 (Recursion)
    - 尾递归原理
    - 经典算法：汉诺塔 (Hanoi Tower)

### 3. 高级特性 (Advanced Features) ✅
- [√] **切片 (Slice)**: 掌握字符串/列表的快速截取 (`s[::-1]` 倒序)。
- [√] **迭代 (Iteration)**: 区分下标循环与元素循环。
- [√] **列表生成式**: 一行代码构建列表 (`[x for x in ... if ...]`)。
- [√] **生成器 (Generator)**:
    - 核心: `yield` 的中断与恢复机制 (Lazy Evaluation)。
    - 实战: 杨辉三角 (利用 `zip` 错位相加)。

### 4. 函数式编程 (Functional Programming) 🚧
- [√] **高阶函数**: 变量可以指向函数，函数可以做参数。
- [√] **map/reduce**:
    - `map`: 一对一映射 (如 `capitalize`)。
    - `reduce`: 累积计算 (如 `str2int`)。
    - **[坑]** 字符串是不可变的 (Immutable)，不能用 `s[0] = 'A'` 修改，只能拼接。
- [√] **匿名函数 (lambda)**: `lambda x: x+1`，一次性小函数。
- [ ] filter
- [ ] sorted
- [ ] 装饰器 (Decorator)

---


## 📝 学习日记

### 2025.11.26 - 函数与递归的突破
1. **Git 实战**：掌握了 `git add` -> `commit` -> `push` 的标准工作流。
2. **CS 基础**：深刻理解了可变对象(List)与不可变对象(Tuple/Str)在内存中的区别。
3. **参数解包**：学会了用 `*args` 和 `**kw` 处理任意参数。
4. **递归思维**：攻克了汉诺塔问题，学会了“宏观思维”（把 n-1 看作整体）。


### 2025.11.27 - 从高级特性到函数式编程
1.  **生成器 (Generator)**: 这是一个难点。理解了 `yield` 就像游戏的“存档暂停”，而不是“结束”。在杨辉三角练习中，学会了 `while True` 配合 `yield` 产生无限数据流。
2.  **列表生成式**: 纠正了 `i.lower(i)` 的错误写法，明白了对象方法不需要手动传 `self`。
3.  **字符串陷阱**: 在做 `normalize` 名字练习时，再次栽在了“字符串不可变”上。切记：字符串只能生成新的，不能改旧的。
4.  **Lambda**: 学会了用 `lambda` 简化代码，但要注意 `lambda` 里只能写表达式，不能写赋值语句。

---

## 🔧 常用命令小抄
- 提交代码：`git add .` -> `git commit -m "备注"` -> `git push`